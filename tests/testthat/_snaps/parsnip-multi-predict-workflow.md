# multi_predict helpers error on untrained workflows

    Code
      multi_predict_args(wf)
    Condition
      Error in `hardhat::extract_fit_parsnip()`:
      ! Can't extract a model fit from an untrained workflow.
      i Do you need to call `fit()`?

---

    Code
      has_multi_predict(wf)
    Condition
      Error in `hardhat::extract_fit_parsnip()`:
      ! Can't extract a model fit from an untrained workflow.
      i Do you need to call `fit()`?

